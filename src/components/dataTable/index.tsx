import React from 'react'

interface dataTableProps {
  title: string;
  lat: string;
  long: string;
}

export const DataTable: React.FC<dataTableProps> = (props) => {
  return(
    <div></div>
  )
}